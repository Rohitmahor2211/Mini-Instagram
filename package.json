{
  "name": "instagram-clone-root",
  "version": "1.0.0",
  "scripts": {
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "build-client": "npm run build --prefix client",
    "build_all": "npm run install-client && npm run build-client && npm run install-server"
  }
}
